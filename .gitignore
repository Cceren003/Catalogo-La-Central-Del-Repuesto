# Credenciales del sync (NUNCA subir)
sync/config.json
sync/removebg.config.json
sync/*.secret.*

# Archivos de proveedor (se bajan/comparten aparte del repo)
sync/reporte_inventario_sample.xls
sync/last_download.xls
sync/downloads/
sync/*.xls
sync/*.xlsx
sync/*.pdf
sync/*.csv
# Excepción: la plantilla de enriquecimientos SÍ va al repo
!sync/enriquecidos-template.xlsx

# Dependencias Node
sync/node_modules/
sync/package-lock.json

# Catálogos de proveedores (archivos pesados, locales del admin)
catalogos-proveedores/

# Logs
sync/*.log

# Estado local de Claude Code (privado a cada dev)
.claude/settings.local.json

# Cache de wrangler (Cloudflare CLI) — local
.wrangler/

